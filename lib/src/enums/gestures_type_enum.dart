enum GesturesType {
  none,
  tap,
  doubleTap,
  longPress,
  drag,
  pinch,
  scroll,
  scrollStart,
  scrollEnd,
}

enum ScrollPhase { start, update, end }

enum NavigationType { none, push, pop, replace, remove }
